{% extends 'base.html.twig' %}

{% block title %}Edit Category
{% endblock %}

{% block body %}
	<div class="card mb-2">
		<div class="card-body">
			<h1 class="card-title">Edit Category</h1>

			{{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

			<a class='btn btn-primary' href="{{ path('category_index', {'id': category.id}) }}">Back To Category List</a>

			{{ include('category/_delete_form.html.twig') }}

		</div>
	</div>
{% endblock %}
