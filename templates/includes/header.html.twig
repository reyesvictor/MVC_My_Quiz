<nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
	<a class="navbar-brand" href="{{path("homepage")}}">QuizGang</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path("homepage") }}">Home<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path("category_index") }}">Categories</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path("quiz_index") }}">Quiz<a></li>
				</ul>
				{% if app.user is defined %}
					{% if app.user %}
						<ul class="navbar-nav ml-auto">
							<li class="nav-item float-right">
								<a class="nav-link" href="{{path('user_show', {'id': app.user.id})}}">My Account:
									{{app.user.username}}</a>
							</li>
							<li class="nav-item  mr-sm-2">
								<a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
							</li>
						{% else %}
							<ul class="navbar-nav ml-auto">
								<li class="nav-item">
									<a class="nav-link" href="{{path("app_login")}}">Login</a>
								</li>
								<ul class="navbar-nav ml-auto">
									<li class="nav-item">
										<a class="nav-link" href="{{path("app_register")}}">Register</a>
									</li>
								{% endif %}
							{% endif %}
							{% if admin is defined %}
								{% if admin %}
									<li class="nav-item">
										<a class="nav-link" href="#">Admin</a>
									</li>
								</ul>
						{% else %}
							</ul>
							{# Close the right navbar #}
						{% endif %}
					{% endif %}
					{# <form class="form-inline my-2 my-lg-0">
																			<input class="form-control mr-sm-2" type="text" placeholder="Search">
																			<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
																		</form> #}
				</li>
			</li>
		</div>
	</nav>
